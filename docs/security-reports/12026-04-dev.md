# Agent Skills Security Scan Report

**Timestamp:** 2026-04-01T07:50:01.630464+00:00

## Summary

- **Total Skills Scanned:** 14
- **Safe Skills:** 14
- **Total Findings:** 56

### Findings by Severity

- **Critical:** 0
- **High:** 0
- **Medium:** 19
- **Low:** 26
- **Info:** 11

## Skill Results


---

### [OK] gitlab-ci-patterns

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/gitlab-ci-patterns

  #### [LOW] Missing optional manifest metadata
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill omits optional manifest fields including license details, compatibility, and allowed-tools. While not inherently malicious, the absence of these fields reduces transparency about intended execution environment and tool scope.
  
  **Code Snippet:**
  ```
  license: Not specified
  compatibility: Not specified
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit manifest metadata for license, compatibility, and allowed-tools to improve clarity and enable stronger policy validation.

  #### [LOW] Referenced internal files are missing or inconsistent
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions reference multiple internal files, but the provided package listing indicates those files are not present and includes inconsistent paths. This is primarily a documentation/integrity issue rather than a direct security threat, but it can hinder review and cause unexpected behavior if the skill depends on absent resources.
  
  **Code Snippet:**
  ```
  Instruction body references:
  - `assets/gitlab-ci.yml.template`
  - `references/pipeline-stages.md`
  
  Provided referenced files marked not found include:
  assets/pipeline-stages.md
  references/pipeline-stages.md
  references/gitlab-ci.yml.template
  GitLab.py
  assets/gitlab-ci.yml.template
  templates/gitlab-ci.yml.template
  templates/pipeline-stages.md
  ```
  
  **Remediation:** Ensure all referenced internal files exist, remove stale references, and align documented paths with actual package contents.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] github-actions-templates

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/github-actions-templates

  #### [LOW] Use of unpinned third-party GitHub Actions in examples
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** Several workflow examples use floating action tags such as `@master`, `@v1`, `@v2`, `@v3`, `@v4`, and `@v5` rather than immutable commit SHAs. In particular, `aquasecurity/trivy-action@master` and `snyk/actions/node@master` create supply-chain exposure because upstream changes could alter behavior unexpectedly. This is a security-hardening concern in the provided templates, though it is presented as example content rather than executable code in the skill package itself.
  
  **Code Snippet:**
  ```
  Examples include `uses: aquasecurity/trivy-action@master` and `uses: snyk/actions/node@master`. Other actions are version-tag pinned only by major release, e.g. `actions/checkout@v4`.
  ```
  
  **Remediation:** Pin third-party GitHub Actions to immutable full-length commit SHAs and document update procedures for reviewing and rotating those pins safely.

  #### [LOW] Missing referenced workflow template files
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill instructions reference multiple internal workflow template files, but none of the referenced files are present in the provided package. This is primarily an integrity and reliability issue: users or agents may expect bundled templates that do not exist, leading to failed execution or incomplete guidance. While not directly malicious, absent referenced artifacts reduce auditability and can obscure what the skill actually intends to provide.
  
  **Code Snippet:**
  ```
  References include `assets/test-workflow.yml`, `assets/deploy-workflow.yml`, `assets/matrix-build.yml`, and `references/common-workflows.md`, all reported as not found.
  ```
  
  **Remediation:** Include all referenced files in the skill package, or remove/update broken references so the package contents accurately match the instructions.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] skill-creator

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/skill-creator

  #### [LOW] Numerous referenced files are missing or inconsistent with instructions
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instruction body references many files and paths that are not present in the provided skill package, including top-level markdown files and various templates/assets. While not inherently malicious, this creates misleading guidance and may cause execution failures or confusion for the consuming agent.
  
  **Code Snippet:**
  ```
  Referenced but not found: REFERENCE.md, FORMS.md, EXAMPLES.md, DOCX-JS.md, OOXML.md, REDLINING.md, templates/api_docs.md, templates/schema.md, templates/frontend-template/, assets/hello-world/, and many others.
  ```
  
  **Remediation:** Remove stale references, ensure all referenced files exist in the packaged skill, and keep path examples clearly marked as illustrative examples rather than actual bundled resources.

  #### [LOW] Instruction/examples may blur illustrative paths with actual package resources
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The SKILL.md body mixes concrete references to bundled files (for example, references/workflows.md and references/output-patterns.md) with many illustrative example paths that do not exist. This can mislead downstream agents into expecting unavailable resources or selecting incorrect paths.
  
  **Code Snippet:**
  ```
  Examples include references to FORMS.md, REFERENCE.md, templates/*.md, assets/*.png, and other files that are not present, while only references/workflows.md and references/output-patterns.md appear to exist.
  ```
  
  **Remediation:** Clearly label illustrative paths as examples, separate examples from actual resource references, and restrict actionable guidance to files that are included in the package.

  #### [LOW] Missing optional allowed-tools manifest field
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The SKILL.md manifest does not specify an allowed-tools field. This is optional per the skill specification, but its absence reduces clarity about intended tool usage and makes policy enforcement less explicit.
  
  **Code Snippet:**
  ```
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add an allowed-tools field to document the minimum required tool permissions, such as [Read, Write, Bash, Python] if those capabilities are expected.


---

### [OK] create-pr

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/create-pr

  #### [LOW] Missing compatibility metadata
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The manifest does not specify compatibility. This makes it less clear where the skill is intended to run and whether dependencies such as git, Bash, and gh CLI are expected to be available. No direct security exploit is present, but missing metadata can contribute to misuse or unexpected execution contexts.
  
  **Code Snippet:**
  ```
  compatibility: Not specified
  ```
  
  **Remediation:** Declare compatibility targets and note external dependencies such as git repository access and GitHub CLI availability.

  #### [LOW] Missing allowed-tools declaration
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill does not declare an allowed-tools field in its manifest. Because the instructions direct use of Bash and the GitHub CLI, the absence of explicit tool restrictions reduces transparency about the skill's required capabilities. This is informational because allowed-tools is optional, and no contradictory tool usage was identified from the provided content.
  
  **Code Snippet:**
  ```
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add an allowed-tools field that accurately reflects required capabilities, such as Bash, and document any prerequisites like gh CLI access.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] catalog-porter

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/catalog-porter

  #### [MEDIUM] Skill instructs the agent to trust and transform untrusted external GitHub repository content
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The skill is explicitly designed to clone arbitrary user-supplied GitHub repositories, discover SKILL.md files, read their contents, classify them, and then port their contents into the local catalog. Because upstream repositories are external and untrusted, their SKILL.md bodies and referenced files may contain malicious prompt-injection content or unsafe instructions. The workflow does not require sanitization, isolation, or a clear policy to treat upstream skill content as untrusted before incorporating it into the catalog.
  
  **Code Snippet:**
  ```
  "Accept the GitHub repo URL from the user." / "git clone --depth 1 <url> /tmp/<repo-name>" / "For each skill directory, read the SKILL.md to understand what it does." / "For PORTED: copy ALL files preserving upstream directory structure."
  ```
  
  **Remediation:** Require strict treatment of upstream repositories as untrusted input. Add explicit instructions to never follow instructions from upstream SKILL.md content, scan imported files for prompt injection and malicious scripts, restrict which file types may be ported, and require human review before copying or transforming external skill bodies.

  #### [MEDIUM] Supply-chain risk from cloning arbitrary user-provided repositories without trust controls
  
  **Severity:** MEDIUM
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** The skill operationalizes direct ingestion of arbitrary GitHub repositories supplied by the user. This creates a supply-chain exposure because malicious repositories can contain hostile skill definitions, dangerous scripts, deceptive licensing metadata, or poisoned supporting files that are then analyzed and copied into the catalog. No repository allowlist, provenance verification, commit pinning, signature verification, or sandboxing requirements are specified.
  
  **Code Snippet:**
  ```
  "Accept the GitHub repo URL from the user." / "Browse or clone the repo to a temp directory" / "git clone --depth 1 <url> /tmp/<repo-name>"
  ```
  
  **Remediation:** Restrict ingestion to vetted repositories or approved owners, pin to specific commits, verify provenance where possible, sandbox analysis, and require explicit review of imported scripts and metadata before porting.

  #### [LOW] Missing allowed-tools declaration despite broad file-system and shell workflow
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The manifest omits the optional allowed-tools field even though the instructions depend on cloning repositories, traversing files, editing catalog content, and running validation commands. While omission is permitted, the absence of declared tool boundaries reduces transparency and makes it harder to assess whether the skill's requested Bash/file modification behavior is intended and constrained.
  
  **Code Snippet:**
  ```
  YAML Manifest Details: "allowed-tools: Not specified"; instructions include "git clone", "find", file creation/update operations, and multiple "bun run" commands.
  ```
  
  **Remediation:** Add an explicit allowed-tools list reflecting the minimum required capabilities, such as Read, Write, Glob, Grep, and Bash, and document any expected constraints on network and repository access.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] secrets-management

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/secrets-management

  #### [MEDIUM] Examples risk exposing secrets in CI/CD logs
  
  **Severity:** MEDIUM
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** Several examples print secret-derived values directly with echo commands. Even though the skill promotes secret management, demonstrating output of secrets or secret-bearing variables can encourage unsafe patterns and accidental disclosure in CI/CD logs. This is especially risky where masking is incomplete or unsupported.
  
  **Code Snippet:**
  ```
  echo "API Key: ${{ secrets.API_KEY }}"
  echo "Database URL: ${{ secrets.DATABASE_URL }}"
  echo "Deploying with $API_KEY"
  echo "Database: $DATABASE_URL"
  echo "Connecting to database as $DB_USERNAME"
  ```
  
  **Remediation:** Replace echo examples with non-sensitive usage patterns, explicitly warn against printing secrets, and demonstrate masking/redaction consistently.

  #### [LOW] Missing optional manifest metadata
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill manifest does not specify license, compatibility, or allowed-tools. While these fields are optional, their absence reduces transparency about intended execution environment and tool boundaries, making security review and safe deployment harder.
  
  **Code Snippet:**
  ```
  license: Not specified
  compatibility: Not specified
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit manifest metadata for license, compatibility, and allowed-tools to improve auditability and constrain expected tool usage.

  #### [LOW] Referenced internal files are missing
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions reference multiple internal documentation files that are not present in the package. This is primarily a documentation/integrity issue rather than an active exploit, but missing references can mislead operators and prevent full review of guidance the skill claims to provide.
  
  **Code Snippet:**
  ```
  Referenced File: references/github-secrets.md (not found)
  Referenced File: references/vault-setup.md (not found)
  Referenced File: templates/github-secrets.md (not found)
  Referenced File: templates/vault-setup.md (not found)
  Referenced File: assets/github-secrets.md (not found)
  Referenced File: assets/vault-setup.md (not found)
  Referenced File: Vault.py (not found)
  Referenced File: AWS.py (not found)
  ```
  
  **Remediation:** Remove stale references or include the referenced files in the package so reviewers and users can validate the complete skill content.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] dependency-upgrade

- **Max Severity:** MEDIUM
- **Findings:** 5
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/dependency-upgrade

  #### [MEDIUM] Instructions rely on untrusted external content for upgrade decisions
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The skill directs the agent/user to fetch and rely on external changelog content from the network. External documentation can contain malicious or misleading instructions, creating an indirect prompt-injection/instruction-manipulation risk if the agent treats fetched content as authoritative guidance.
  
  **Code Snippet:**
  ```
  curl https://raw.githubusercontent.com/facebook/react/master/CHANGELOG.md
  ```
  
  **Remediation:** Treat externally fetched documentation as untrusted data only. Summarize it defensively, avoid following embedded instructions automatically, and prefer vendored or pinned documentation snapshots when possible.

  #### [MEDIUM] Unpinned remote codemod execution from external URLs
  
  **Severity:** MEDIUM
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** SKILL.md
  
  **Description:** The skill instructs users to execute jscodeshift transforms directly from remote GitHub raw URLs. This creates a supply-chain risk because code fetched at execution time may change, be compromised upstream, or be replaced with malicious content, leading to arbitrary code execution during migration.
  
  **Code Snippet:**
  ```
  npx jscodeshift -t https://raw.githubusercontent.com/reactjs/react-codemod/master/transforms/rename-unsafe-lifecycles.js src/
  ```
  
  **Remediation:** Vendor trusted codemod files into the skill package or pin to immutable commit-specific URLs with checksum verification. Require user review of transform code before execution.

  #### [LOW] Referenced files listed in instructions are missing or inconsistent
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill references numerous local support files that are absent, and the analyzed reference list includes inconsistent paths such as templates/* and lock.py that are not mentioned in the visible instructions. While not directly malicious, this degrades trustworthiness and can mislead the agent or user about available resources.
  
  **Code Snippet:**
  ```
  Resources section references files such as references/semver.md, references/compatibility-matrix.md, assets/upgrade-checklist.md, but all reported referenced files are marked '(not found)'.
  ```
  
  **Remediation:** Remove nonexistent references or include the files in the package. Ensure referenced-file lists are accurate and consistent with the manifest and instruction body.

  #### [LOW] Missing optional manifest metadata reduces transparency
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill omits license, compatibility, and allowed-tools metadata. Although allowed-tools is optional, the absence of these fields reduces clarity about expected execution boundaries and environment assumptions for a skill that recommends shell commands and package-management operations.
  
  **Code Snippet:**
  ```
  license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit metadata for license, compatibility, and allowed-tools to improve transparency and constrain expected agent behavior.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] doc-coauthoring

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/doc-coauthoring

  #### [LOW] Referenced files listed but not present in package
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  
  **Description:** The skill metadata indicates referenced files `real.py` and `messaging.py`, but they were not provided with the package contents. Missing referenced files reduce auditability and can hide intended behavior from review, though no executable code was available here to confirm misuse.
  
  **Code Snippet:**
  ```
  Referenced Files: real.py, messaging.py; both marked as not found.
  ```
  
  **Remediation:** Include all referenced files in the skill package, or remove stale references. Ensure reviewers can inspect every file the instructions depend on.

  #### [LOW] Broad activation language may increase unnecessary triggering
  
  **Severity:** LOW
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The skill description and trigger conditions are broad (e.g., documentation, proposals, specs, RFCs, write-ups). While not overtly deceptive, this wide trigger surface can cause the skill to activate for many generic writing tasks beyond a narrowly scoped capability.
  
  **Code Snippet:**
  ```
  Trigger conditions include broad phrases such as "write a doc", "draft a proposal", "create a spec", and similar documentation tasks.
  ```
  
  **Remediation:** Narrow trigger conditions and description to the specific workflow supported, with clearer boundaries on when the skill should or should not activate.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] github-triage

- **Max Severity:** MEDIUM
- **Findings:** 5
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/github-triage

  #### [MEDIUM] Manifest tool restrictions do not cover write-capable GitHub CLI operations executed through Bash/Python
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill claims that write actions require explicit confirmation, but its allowed-tools model permits `Bash` and `Python`, which are then used to invoke `gh issue comment`, `gh issue close`, and `gh pr merge`. This means repository-modifying actions are achievable indirectly through broad execution tools, weakening the effectiveness of the declared tool restrictions and creating an authorization gap if gating logic is bypassed or misapplied.
  
  **Code Snippet:**
  ```
  Examples in instructions: `gh issue comment {number} --repo {REPO} --body "YOUR_COMMENT"`, `gh issue close {number} --repo {REPO}`, `gh pr merge {number} --repo {REPO} --squash --auto`
  ```
  
  **Remediation:** Separate read-only and write-capable modes at the tool-policy level. Avoid granting unrestricted Bash/Python for workflows that should be read-mostly, or add hard runtime enforcement preventing write-capable `gh` subcommands unless an external authorization mechanism approves the exact action.

  #### [LOW] Referenced files listed in instructions are missing or inconsistent
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The package references files `the.py`, `codebase.py`, and `determining.py`, but they are not present. While not directly malicious, inconsistent or dangling references can mislead operators, complicate auditing, and obscure the true behavior of the skill package.
  
  **Code Snippet:**
  ```
  Referenced Files: the.py (not found), codebase.py (not found), determining.py (not found)
  ```
  
  **Remediation:** Remove nonexistent references from the instructions or include the missing files. Ensure all referenced resources are bundled and documented accurately.

  #### [LOW] Allowed-tools declaration includes nonstandard/undeclared tool names that may bypass intended restrictions
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The manifest declares allowed-tools including `TaskCreate`, `TaskUpdate`, `task`, and `background_output`, which are not part of the documented standard tool set described in the analysis framework. This creates a tool-governance ambiguity: the skill relies on privileged orchestration capabilities beyond the normal agent tool list, increasing the risk of unauthorized tool use or policy mismatch across environments.
  
  **Code Snippet:**
  ```
  allowed-tools: Bash, Python, Read, Grep, Glob, TaskCreate, TaskUpdate, task, background_output
  ```
  
  **Remediation:** Restrict `allowed-tools` to documented, supported tool names for the target platform, or clearly document and validate these additional tools as platform-approved capabilities. Enforce runtime checks so the skill cannot invoke undeclared or unsupported tools.

  #### [MEDIUM] Bash: tainted data flows to dangerous sink
  
  **Severity:** MEDIUM
  **Category:** command_injection
  **Rule ID:** BEHAVIOR_BASH_TAINT_FLOW
  **Location:** SKILL.md:41
  
  **Description:** Variable $REPO (line 1) flows to `python3` at line 4.
  
  **Code Snippet:**
  ```
  python3 scripts/gh_fetch.py all --repo "$REPO" --state open --output json
  ```
  
  **Remediation:** Review the data flow from source to sink. Avoid sending sensitive or untrusted data to network or execution commands.

  #### [LOW] Script dependencies use range pinning rather than exact version pinning
  
  **Severity:** LOW
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  **Location:** scripts/gh_fetch.py
  
  **Description:** The bundled Python script specifies dependency ranges (`typer>=0.24.1,<0.25.0`, `rich>=14.3.3,<15.0.0`) rather than exact versions. This leaves room for supply-chain variability across installations and reduces reproducibility.
  
  **Code Snippet:**
  ```
  dependencies = ["typer>=0.24.1,<0.25.0", "rich>=14.3.3,<15.0.0"]
  ```
  
  **Remediation:** Pin dependencies to exact known-good versions and consider adding integrity verification or a lockfile-based workflow.


---

### [OK] git-master

- **Max Severity:** MEDIUM
- **Findings:** 5
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/git-master

  #### [MEDIUM] Unsafe autonomy and destructive workflow guidance without explicit confirmation gates
  
  **Severity:** MEDIUM
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instructions direct the agent to execute git operations, including history rewriting and potentially destructive recovery commands, with mandatory/blocking language. Although some safety notes are present, the skill still encourages autonomous execution of impactful actions such as rebases, reset/rebuild flows, cherry-picks, force-push preparation, and reset --hard recovery without requiring explicit per-action user confirmation.
  
  **Code Snippet:**
  ```
  Examples include: 'Execute ALL of the following commands IN PARALLEL', 'Your DEFAULT behavior is to CREATE MULTIPLE COMMITS', 'CONSIDER RESET & REBUILD', 'git reset --soft $(git merge-base HEAD main)', 'git reset --hard <lost-hash>', and push guidance for '--force-with-lease'.
  ```
  
  **Remediation:** Require explicit user confirmation before any state-changing git command, especially commit, rebase, reset, cherry-pick, worktree remove, and force-push related operations. Prefer analysis-only planning by default.

  #### [MEDIUM] Over-broad activation and capability inflation in manifest description
  
  **Severity:** MEDIUM
  **Category:** skill_discovery_abuse
  **Rule ID:** LLM_SKILL_DISCOVERY_ABUSE
  **Location:** SKILL.md
  
  **Description:** The skill description uses coercive discovery language such as 'MUST USE for ANY git operations' and 'STRONGLY RECOMMENDED' with a broad trigger list. This can manipulate skill selection and increase unwanted activation beyond a narrowly scoped need, which is a protocol/discovery abuse pattern.
  
  **Code Snippet:**
  ```
  Description: 'MUST USE for ANY git operations... STRONGLY RECOMMENDED... Triggers: 'commit', 'rebase', 'squash', 'cherry-pick', 'worktree', 'reflog', 'who wrote', 'when was X added', 'find the commit that'.'
  ```
  
  **Remediation:** Rewrite the description to be capability-scoped and non-coercive. Remove 'MUST USE'/'STRONGLY RECOMMENDED' language and keep triggers limited to concrete, justified use cases.

  #### [LOW] Missing optional manifest metadata reduces transparency
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill omits license, compatibility, and allowed-tools metadata. While allowed-tools is optional, missing metadata makes it harder to assess intended execution boundaries and environmental assumptions.
  
  **Code Snippet:**
  ```
  license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit license, compatibility targets, and allowed-tools to improve transparency and constrain expected agent behavior.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [MEDIUM] Bash: tainted data flows to dangerous sink
  
  **Severity:** MEDIUM
  **Category:** command_injection
  **Rule ID:** BEHAVIOR_BASH_TAINT_FLOW
  **Location:** SKILL.md:499
  
  **Description:** Variable $MERGE_BASE (line 8) flows to `sh` at line 9.
  
  **Code Snippet:**
  ```
  GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash $MERGE_BASE
  ```
  
  **Remediation:** Review the data flow from source to sink. Avoid sending sensitive or untrusted data to network or execution commands.


---

### [OK] mcp-builder

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/mcp-builder

  #### [MEDIUM] Dependency installation guidance uses unpinned packages
  
  **Severity:** MEDIUM
  **Category:** supply_chain_attack
  **Rule ID:** LLM_SUPPLY_CHAIN_ATTACK
  
  **Description:** The evaluation guide instructs users to install dependencies with unpinned package versions (`pip install anthropic mcp`). Unpinned dependencies increase supply-chain risk by allowing unexpected upstream changes, malicious package compromise, or non-reproducible environments.
  
  **Code Snippet:**
  ```
  `pip install anthropic mcp`
  ```
  
  **Remediation:** Pin exact versions in a requirements file or installation command (for example, `anthropic==...` and `mcp==...`) and preferably use hash-locked dependencies for reproducibility.

  #### [MEDIUM] Skill instructs fetching external documentation, creating indirect prompt-injection exposure
  
  **Severity:** MEDIUM
  **Category:** prompt_injection
  **Rule ID:** LLM_PROMPT_INJECTION
  **Location:** SKILL.md
  
  **Description:** The instructions direct the agent to fetch and use external content from multiple remote URLs, including the MCP specification site and GitHub raw READMEs. Although the skill includes some cautionary language, external documentation remains untrusted input and may contain malicious or conflicting instructions, unsafe code snippets, or manipulative content. This creates an indirect prompt-injection risk because the workflow explicitly incorporates remote content into agent decision-making.
  
  **Code Snippet:**
  ```
  "Start with the sitemap to find relevant pages: https://modelcontextprotocol.io/sitemap.xml"
  "Use WebFetch to load https://raw.githubusercontent.com/modelcontextprotocol/typescript-sdk/main/README.md"
  "Use WebFetch to load https://raw.githubusercontent.com/modelcontextprotocol/python-sdk/main/README.md"
  ```
  
  **Remediation:** Constrain external fetches to vetted, pinned resources where possible; treat fetched content as untrusted data only; require explicit validation/parsing rules; avoid following instructions from fetched pages; prefer vendored internal references over live remote documentation.

  #### [MEDIUM] Allowed-tools manifest omits Python despite bundled Python scripts
  
  **Severity:** MEDIUM
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The manifest declares allowed-tools as Read, Write, Grep, Glob, Bash, but the skill package includes executable Python scripts intended for use (`scripts/evaluation.py`, `scripts/connections.py`). This creates a cross-component inconsistency: the package behavior relies on Python execution that is not declared in the manifest. Such undeclared tool usage can mislead the agent about the skill's effective privileges and execution model.
  
  **Code Snippet:**
  ```
  allowed-tools: Read, Write, Grep, Glob, Bash
  
  Bundled executable files:
  - scripts/evaluation.py
  - scripts/connections.py
  ```
  
  **Remediation:** Add `Python` to `allowed-tools` if Python execution is required, or remove/replace Python scripts so the package behavior matches the declared tool set.

  #### [LOW] Referenced files list includes nonexistent files
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The instruction metadata references several files that do not exist in the package (`its.py`, `external.py`, `concise.py`, `the.py`). While not directly exploitable on its own, this can mislead agents, create ambiguity during skill execution, and increase the chance of accidental fallback to unintended files or user-provided content.
  
  **Code Snippet:**
  ```
  Referenced files: its.py, external.py, concise.py, the.py
  
  Marked as not found in provided package contents.
  ```
  
  **Remediation:** Remove nonexistent references from the skill or include the missing files explicitly. Ensure all referenced resources are accurate and bundled with the package.


---

### [OK] docs-changelog

- **Max Severity:** MEDIUM
- **Findings:** 4
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/docs-changelog

  #### [LOW] Missing optional manifest metadata reduces tool-use clarity
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The skill omits optional manifest fields such as `allowed-tools`, `license`, and `compatibility`. This is not inherently malicious, but it reduces transparency about what agent tools the skill is expected to use and what environments it supports, making policy validation and review harder.
  
  **Code Snippet:**
  ```
  YAML Manifest Details: license: Not specified; compatibility: Not specified; allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit `allowed-tools`, `license`, and `compatibility` fields to improve reviewability and constrain expected agent behavior.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)

  #### [MEDIUM] File extension does not match actual content type
  
  **Severity:** MEDIUM
  **Category:** obfuscation
  **Rule ID:** FILE_MAGIC_MISMATCH
  **Location:** references/latest_template.md
  
  **Description:** File 'latest_template.md' extension (.md) suggests one format but Magika detected a different text format: Handlebars source (handlebars). This may indicate content obfuscation or a misnamed file.
  
  **Remediation:** Rename the file to match its actual content type, or remove it if it appears malicious.

  #### [MEDIUM] File extension does not match actual content type
  
  **Severity:** MEDIUM
  **Category:** obfuscation
  **Rule ID:** FILE_MAGIC_MISMATCH
  **Location:** references/preview_template.md
  
  **Description:** File 'preview_template.md' extension (.md) suggests one format but Magika detected a different text format: Handlebars source (handlebars). This may indicate content obfuscation or a misnamed file.
  
  **Remediation:** Rename the file to match its actual content type, or remove it if it appears malicious.


---

### [OK] javascript-testing-patterns

- **Max Severity:** LOW
- **Findings:** 2
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/javascript-testing-patterns

  #### [LOW] Missing optional manifest metadata
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill omits optional manifest fields such as license, compatibility, and allowed-tools. This does not indicate malicious behavior by itself, but it reduces transparency about intended execution environment and tool usage constraints.
  
  **Code Snippet:**
  ```
  license: Not specified
  compatibility: Not specified
  allowed-tools: Not specified
  ```
  
  **Remediation:** Add explicit manifest metadata for license, compatibility, and allowed-tools to improve transparency and enable clearer policy enforcement.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### [OK] docs-writer

- **Max Severity:** LOW
- **Findings:** 3
- **Directory:** /home/runner/work/agent-toolbox/agent-toolbox/.agents/skills/docs-writer

  #### [LOW] Missing optional manifest metadata reduces transparency
  
  **Severity:** LOW
  **Category:** harmful_content
  **Rule ID:** LLM_HARMFUL_CONTENT
  **Location:** SKILL.md
  
  **Description:** The skill does not specify `allowed-tools`, `license`, or `compatibility`. This is not itself malicious, and there is no evidence of hidden scripts or unsafe tool use. However, missing metadata reduces clarity about intended tool access and packaging provenance.
  
  **Code Snippet:**
  ```
  YAML manifest details: `license: Not specified`, `compatibility: Not specified`, `allowed-tools: Not specified`.
  ```
  
  **Remediation:** Add optional metadata fields to improve transparency, especially `allowed-tools` to declare expected tool usage and `license`/`compatibility` to clarify provenance and runtime expectations.

  #### [LOW] Referenced file list contains suspicious/invalid entry not used by core instructions
  
  **Severity:** LOW
  **Category:** unauthorized_tool_use
  **Rule ID:** LLM_UNAUTHORIZED_TOOL_USE
  **Location:** SKILL.md
  
  **Description:** The provided referenced-file inventory includes an unexpected Python file entry (`that.py`) and multiple non-existent alternate-path references (`templates/` and `assets/` variants) that do not align with the SKILL.md body, which references only `references/` files. While no executable script content is present, this inconsistency can confuse analysis and may indicate packaging errors or accidental inclusion of unrelated files.
  
  **Code Snippet:**
  ```
  SKILL.md references: `references/PROFILE-GENERIC.md`, `references/PROFILE-NEXTJS.md`, `references/PROFILE-ANGULAR.md`, `references/TEMPLATES.md`, `references/CORE-CHECKLIST.md`.
  Referenced file inventory additionally lists: `that.py` (not found), `templates/...` and `assets/...` variants (not found).
  ```
  
  **Remediation:** Clean the package manifest/reference inventory so it lists only files actually referenced by SKILL.md. Remove stray entries such as `that.py`, and ensure referenced paths are accurate and exist in the package.

  #### [INFO] Skill does not specify a license
  
  **Severity:** INFO
  **Category:** policy_violation
  **Rule ID:** MANIFEST_MISSING_LICENSE
  **Location:** SKILL.md
  
  **Description:** Skill manifest does not include a 'license' field. Specifying a license helps users understand usage terms.
  
  **Remediation:** Add 'license' field to SKILL.md frontmatter (e.g., MIT, Apache-2.0)


---

### Cross-Skill Findings

- **Findings:** 5

  #### [MEDIUM] Potential data relay attack pattern detected
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_DATA_RELAY
  **Location:** (cross-skill analysis)
  
  **Description:** Skills appear to form a data relay chain. Collectors (gitlab-ci-patterns, github-actions-templates, skill-creator, create-pr, catalog-porter, secrets-management, dependency-upgrade, doc-coauthoring, github-triage, git-master, mcp-builder, docs-changelog, javascript-testing-patterns, docs-writer) access sensitive data while exfiltrators (github-actions-templates) send data to external destinations. This pattern may indicate a coordinated attack.
  
  **Remediation:** Review these skills together to ensure they are not collaborating to exfiltrate sensitive data. Consider disabling one or both skills.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'app.example.com' is referenced by 2 skills: gitlab-ci-patterns, github-actions-templates. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'example.com' is referenced by 2 skills: mcp-builder, javascript-testing-patterns. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [MEDIUM] Multiple skills reference the same external domain
  
  **Severity:** MEDIUM
  **Category:** data_exfiltration
  **Rule ID:** CROSS_SKILL_SHARED_URL
  **Location:** (cross-skill analysis)
  
  **Description:** Domain 'api.example.com' is referenced by 2 skills: mcp-builder, javascript-testing-patterns. Multiple skills pointing to the same external resource may indicate coordinated C2 or exfiltration.
  
  **Remediation:** Review why multiple skills reference this domain and ensure it is a legitimate, trusted resource.

  #### [LOW] Skills have complementary descriptions
  
  **Severity:** LOW
  **Category:** social_engineering
  **Rule ID:** CROSS_SKILL_COMPLEMENTARY_TRIGGERS
  **Location:** (cross-skill analysis)
  
  **Description:** Skill 'git-master' (collector) and 'doc-coauthoring' (sender) have complementary descriptions with shared context: context, use, when. This may be intentional design or could indicate coordinated behavior.
  
  **Remediation:** Review these skills to ensure they are not designed to work together maliciously
